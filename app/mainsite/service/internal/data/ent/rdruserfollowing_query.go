// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/fxkt-tech/raiden/app/mainsite/service/internal/data/ent/predicate"
	"github.com/fxkt-tech/raiden/app/mainsite/service/internal/data/ent/rdruserfollowing"
)

// RdRUserFollowingQuery is the builder for querying RdRUserFollowing entities.
type RdRUserFollowingQuery struct {
	config
	ctx        *QueryContext
	order      []OrderFunc
	inters     []Interceptor
	predicates []predicate.RdRUserFollowing
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RdRUserFollowingQuery builder.
func (rrfq *RdRUserFollowingQuery) Where(ps ...predicate.RdRUserFollowing) *RdRUserFollowingQuery {
	rrfq.predicates = append(rrfq.predicates, ps...)
	return rrfq
}

// Limit the number of records to be returned by this query.
func (rrfq *RdRUserFollowingQuery) Limit(limit int) *RdRUserFollowingQuery {
	rrfq.ctx.Limit = &limit
	return rrfq
}

// Offset to start from.
func (rrfq *RdRUserFollowingQuery) Offset(offset int) *RdRUserFollowingQuery {
	rrfq.ctx.Offset = &offset
	return rrfq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rrfq *RdRUserFollowingQuery) Unique(unique bool) *RdRUserFollowingQuery {
	rrfq.ctx.Unique = &unique
	return rrfq
}

// Order specifies how the records should be ordered.
func (rrfq *RdRUserFollowingQuery) Order(o ...OrderFunc) *RdRUserFollowingQuery {
	rrfq.order = append(rrfq.order, o...)
	return rrfq
}

// First returns the first RdRUserFollowing entity from the query.
// Returns a *NotFoundError when no RdRUserFollowing was found.
func (rrfq *RdRUserFollowingQuery) First(ctx context.Context) (*RdRUserFollowing, error) {
	nodes, err := rrfq.Limit(1).All(setContextOp(ctx, rrfq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{rdruserfollowing.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rrfq *RdRUserFollowingQuery) FirstX(ctx context.Context) *RdRUserFollowing {
	node, err := rrfq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RdRUserFollowing ID from the query.
// Returns a *NotFoundError when no RdRUserFollowing ID was found.
func (rrfq *RdRUserFollowingQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = rrfq.Limit(1).IDs(setContextOp(ctx, rrfq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{rdruserfollowing.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rrfq *RdRUserFollowingQuery) FirstIDX(ctx context.Context) int64 {
	id, err := rrfq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RdRUserFollowing entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RdRUserFollowing entity is found.
// Returns a *NotFoundError when no RdRUserFollowing entities are found.
func (rrfq *RdRUserFollowingQuery) Only(ctx context.Context) (*RdRUserFollowing, error) {
	nodes, err := rrfq.Limit(2).All(setContextOp(ctx, rrfq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{rdruserfollowing.Label}
	default:
		return nil, &NotSingularError{rdruserfollowing.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rrfq *RdRUserFollowingQuery) OnlyX(ctx context.Context) *RdRUserFollowing {
	node, err := rrfq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RdRUserFollowing ID in the query.
// Returns a *NotSingularError when more than one RdRUserFollowing ID is found.
// Returns a *NotFoundError when no entities are found.
func (rrfq *RdRUserFollowingQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = rrfq.Limit(2).IDs(setContextOp(ctx, rrfq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{rdruserfollowing.Label}
	default:
		err = &NotSingularError{rdruserfollowing.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rrfq *RdRUserFollowingQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := rrfq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RdRUserFollowings.
func (rrfq *RdRUserFollowingQuery) All(ctx context.Context) ([]*RdRUserFollowing, error) {
	ctx = setContextOp(ctx, rrfq.ctx, "All")
	if err := rrfq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RdRUserFollowing, *RdRUserFollowingQuery]()
	return withInterceptors[[]*RdRUserFollowing](ctx, rrfq, qr, rrfq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rrfq *RdRUserFollowingQuery) AllX(ctx context.Context) []*RdRUserFollowing {
	nodes, err := rrfq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RdRUserFollowing IDs.
func (rrfq *RdRUserFollowingQuery) IDs(ctx context.Context) ([]int64, error) {
	var ids []int64
	ctx = setContextOp(ctx, rrfq.ctx, "IDs")
	if err := rrfq.Select(rdruserfollowing.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rrfq *RdRUserFollowingQuery) IDsX(ctx context.Context) []int64 {
	ids, err := rrfq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rrfq *RdRUserFollowingQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rrfq.ctx, "Count")
	if err := rrfq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rrfq, querierCount[*RdRUserFollowingQuery](), rrfq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rrfq *RdRUserFollowingQuery) CountX(ctx context.Context) int {
	count, err := rrfq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rrfq *RdRUserFollowingQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rrfq.ctx, "Exist")
	switch _, err := rrfq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rrfq *RdRUserFollowingQuery) ExistX(ctx context.Context) bool {
	exist, err := rrfq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RdRUserFollowingQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rrfq *RdRUserFollowingQuery) Clone() *RdRUserFollowingQuery {
	if rrfq == nil {
		return nil
	}
	return &RdRUserFollowingQuery{
		config:     rrfq.config,
		ctx:        rrfq.ctx.Clone(),
		order:      append([]OrderFunc{}, rrfq.order...),
		inters:     append([]Interceptor{}, rrfq.inters...),
		predicates: append([]predicate.RdRUserFollowing{}, rrfq.predicates...),
		// clone intermediate query.
		sql:  rrfq.sql.Clone(),
		path: rrfq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UID int32 `json:"uid,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RdRUserFollowing.Query().
//		GroupBy(rdruserfollowing.FieldUID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rrfq *RdRUserFollowingQuery) GroupBy(field string, fields ...string) *RdRUserFollowingGroupBy {
	rrfq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RdRUserFollowingGroupBy{build: rrfq}
	grbuild.flds = &rrfq.ctx.Fields
	grbuild.label = rdruserfollowing.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UID int32 `json:"uid,omitempty"`
//	}
//
//	client.RdRUserFollowing.Query().
//		Select(rdruserfollowing.FieldUID).
//		Scan(ctx, &v)
func (rrfq *RdRUserFollowingQuery) Select(fields ...string) *RdRUserFollowingSelect {
	rrfq.ctx.Fields = append(rrfq.ctx.Fields, fields...)
	sbuild := &RdRUserFollowingSelect{RdRUserFollowingQuery: rrfq}
	sbuild.label = rdruserfollowing.Label
	sbuild.flds, sbuild.scan = &rrfq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RdRUserFollowingSelect configured with the given aggregations.
func (rrfq *RdRUserFollowingQuery) Aggregate(fns ...AggregateFunc) *RdRUserFollowingSelect {
	return rrfq.Select().Aggregate(fns...)
}

func (rrfq *RdRUserFollowingQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rrfq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rrfq); err != nil {
				return err
			}
		}
	}
	for _, f := range rrfq.ctx.Fields {
		if !rdruserfollowing.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rrfq.path != nil {
		prev, err := rrfq.path(ctx)
		if err != nil {
			return err
		}
		rrfq.sql = prev
	}
	return nil
}

func (rrfq *RdRUserFollowingQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RdRUserFollowing, error) {
	var (
		nodes = []*RdRUserFollowing{}
		_spec = rrfq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RdRUserFollowing).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RdRUserFollowing{config: rrfq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rrfq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (rrfq *RdRUserFollowingQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rrfq.querySpec()
	_spec.Node.Columns = rrfq.ctx.Fields
	if len(rrfq.ctx.Fields) > 0 {
		_spec.Unique = rrfq.ctx.Unique != nil && *rrfq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rrfq.driver, _spec)
}

func (rrfq *RdRUserFollowingQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   rdruserfollowing.Table,
			Columns: rdruserfollowing.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt64,
				Column: rdruserfollowing.FieldID,
			},
		},
		From:   rrfq.sql,
		Unique: true,
	}
	if unique := rrfq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := rrfq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, rdruserfollowing.FieldID)
		for i := range fields {
			if fields[i] != rdruserfollowing.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := rrfq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rrfq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rrfq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rrfq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rrfq *RdRUserFollowingQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rrfq.driver.Dialect())
	t1 := builder.Table(rdruserfollowing.Table)
	columns := rrfq.ctx.Fields
	if len(columns) == 0 {
		columns = rdruserfollowing.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rrfq.sql != nil {
		selector = rrfq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rrfq.ctx.Unique != nil && *rrfq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rrfq.predicates {
		p(selector)
	}
	for _, p := range rrfq.order {
		p(selector)
	}
	if offset := rrfq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rrfq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RdRUserFollowingGroupBy is the group-by builder for RdRUserFollowing entities.
type RdRUserFollowingGroupBy struct {
	selector
	build *RdRUserFollowingQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rrfgb *RdRUserFollowingGroupBy) Aggregate(fns ...AggregateFunc) *RdRUserFollowingGroupBy {
	rrfgb.fns = append(rrfgb.fns, fns...)
	return rrfgb
}

// Scan applies the selector query and scans the result into the given value.
func (rrfgb *RdRUserFollowingGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rrfgb.build.ctx, "GroupBy")
	if err := rrfgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RdRUserFollowingQuery, *RdRUserFollowingGroupBy](ctx, rrfgb.build, rrfgb, rrfgb.build.inters, v)
}

func (rrfgb *RdRUserFollowingGroupBy) sqlScan(ctx context.Context, root *RdRUserFollowingQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rrfgb.fns))
	for _, fn := range rrfgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rrfgb.flds)+len(rrfgb.fns))
		for _, f := range *rrfgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rrfgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rrfgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RdRUserFollowingSelect is the builder for selecting fields of RdRUserFollowing entities.
type RdRUserFollowingSelect struct {
	*RdRUserFollowingQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rrfs *RdRUserFollowingSelect) Aggregate(fns ...AggregateFunc) *RdRUserFollowingSelect {
	rrfs.fns = append(rrfs.fns, fns...)
	return rrfs
}

// Scan applies the selector query and scans the result into the given value.
func (rrfs *RdRUserFollowingSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rrfs.ctx, "Select")
	if err := rrfs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RdRUserFollowingQuery, *RdRUserFollowingSelect](ctx, rrfs.RdRUserFollowingQuery, rrfs, rrfs.inters, v)
}

func (rrfs *RdRUserFollowingSelect) sqlScan(ctx context.Context, root *RdRUserFollowingQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rrfs.fns))
	for _, fn := range rrfs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rrfs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rrfs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
